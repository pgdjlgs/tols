#!/bin/bash
# pass: switch between windows and void linux

case $1 in
  wd)
    echo "booting windows..."
    sudo grub-reboot 2 && sudo reboot
    ;;
  vo)
    echo "booting void linux..."
    sudo grub-reboot 0 && sudo reboot
    ;;
  *)
    echo "usage: pass wd | pass vo"
    ;;
esac
