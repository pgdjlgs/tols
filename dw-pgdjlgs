#!/bin/bash

# اكتشاف المستخدم الحقيقي حتى لو تم استخدام sudo
REAL_USER=${SUDO_USER:-$USER}
REPO_DIR="/home/$REAL_USER/dw-pgdjlgs"

if [ "$1" == "-s" ]; then
    TOOL_NAME=$2
    if [ -f "$REPO_DIR/$TOOL_NAME" ]; then
        echo "⚙️ جاري تثبيت الأداة: $TOOL_NAME ..."
        chmod +x "$REPO_DIR/$TOOL_NAME"
        cp "$REPO_DIR/$TOOL_NAME" /usr/local/bin/
        echo "✅ تم التثبيت! يمكنك الآن كتابة '$TOOL_NAME' في أي مكان."
    else
        echo "❌ خطأ: لم يتم العثور على '$TOOL_NAME' في $REPO_DIR"
    fi
elif [ "$1" == "-u" ]; then
    echo "🔄 تحديث المستودع من GitHub..."
    cd "$REPO_DIR" && git pull
else
    echo "❓ الاستخدام: sudo dw-pgdjlgs -s [الأداة] أو -u للتحديث"
fi
